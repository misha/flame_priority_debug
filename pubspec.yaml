name: priority_debug
description: Project for debugging priority behavior in Flame.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.0.6 <4.0.0"
dependencies:
  cupertino_icons: ^1.0.2
  flame: ^1.8.2
  flutter:
    sdk: flutter
dev_dependencies:
  flame_lint: ^1.1.0
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
